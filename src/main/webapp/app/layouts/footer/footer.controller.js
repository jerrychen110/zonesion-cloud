/*(function() {
    'use strict';

    angular
        .module('zonesionCloudApplicationApp')
        .controller('footerController', footerController);

    footerController.$inject = ['$state', 'Auth', 'Principal', 'ProfileService', 'LoginService','$scope','$rootScope'];

    function footerController ($state, Auth, Principal, ProfileService, LoginService,$scope,$rootScope) {
        var vm = this;
    }
})();*/
